name: azure-functions-connectors-sample
metadata:
  template: azure-functions-connectors-sample@0.0.1-beta
hooks:
  postdeploy:
    windows:
      shell: pwsh
      run: ./infra/scripts/postdeploy.ps1
    posix:
      shell: sh
      run: ./infra/scripts/postdeploy.sh
services:
  function-app:
    project: ./function-app
    language: dotnet
    host: function
